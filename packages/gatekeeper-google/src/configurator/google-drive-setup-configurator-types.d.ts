export type GoogleDriveSetupConfiguratorValues = {
  /** Optional Google Drive folder URL pasted by the user. Empty = My Drive root. */
  folderUrl?: string;
};

export interface GoogleDriveSetupConfiguratorRpc {}
